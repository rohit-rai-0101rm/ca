import React from 'react';


const SectionTitle6 = ({subtitle, title, description, textAlignment, textColor}) => {
    return (
        <div className={`section-heading ${textAlignment} ${textColor}`}>
            <div className="subtitle"></div>
        
        
            <p ></p>
        </div>
    )
}

export default SectionTitle6;